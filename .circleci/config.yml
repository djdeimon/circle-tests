version: '2.1'
jobs:
  build:
    working_directory: /mnt/ramdisk
    docker:
      - image: cimg/node:17.2.0
    steps:
      - checkout
      - run: yarn --save
  test:
    working_directory: /mnt/ramdisk
    docker:
      - image: cimg/node:17.2.0
    steps:
      - run: yarn test

workflows:
  test:
    jobs:
      - build
      - test:
          requires:
            - build


